---
name: use_bun_not_npm
description: User uses Bun as package manager, not npm
type: feedback
---

Always use `bun` instead of `npm` for package management commands (bun install, bun add, bun run, etc.).

**Why:** User preference and project setup - the project was initialized with Bun.

**How to apply:** Any time you need to install packages, run scripts, or do package management, use `bun` commands.
